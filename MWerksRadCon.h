// $Log: MWerksRadCon.h,v $// Revision 1.2  1999/10/18 11:22:48  rdmp1c// CW4 compatible////#ifdef __MWERKS__ // ¥ Metrowerks specific options	#ifdef __INTEL__			// Win32		#define Windows								// Windows		#define __WIN32__							// MetroWerks only supports Win32		#define _export								// dummy this out		#define stricmp		_stricmp				// MSL equivalent		#define M_PI		3.14159265358979323846	// define pi		#define NOMINMAX							// Prevent min and max being defined in Windows header.													// Borland checks for cplusplus, Metrowerks doesn't		#include "ansi_prefix.win32.h"				// MSL ANSI definitions		#include "VString.h"						// Missing ANSI string functions			#endif /* __INTEL__ */	#ifdef macintosh			// MacOS		#define Macintosh							// Macintosh		#define M_PI		3.14159265358979323846	// define pi						// ¥ C++ language options		// Many of these options can be set in the project settings dialog box,		// but just to make sure we set them here. This avoids problems of forgetting		// to set the right options, which can lead to much grief later on with MSL libraries.		// (-- damn right!  - MAC 12/4/99)				// Need additional Apple keywords to compile ToolBox code		#if (__option (only_std_keywords))			#pragma only_std_keywords off		#endif 		// ¥ Use Metrowerks Standard Library (MSL) with CW PR1 and above		#if 1 // 1 to use MSL, 0 to use obsolete ANSI libraries			#define USE_MSL	(__MWERKS__ >= 0x1800)		#endif		#if (USE_MSL)								// Metrowerks Standard Libraries					// MAC's additions (12/4/99):			#if (__MWERKS__ > 0x2000) // RDMP Don't do this for CW 2 or earlier				#include <MacHeaders.h>	// added by MAC, 12/4/99.				#include <stdlib.h>	// for some backward compatibility with things like size_t			#endif			// .. to here.						// MSL			#pragma bool on			#pragma exceptions on			#pragma RTTI on			#include <ansi_prefix.mac.h>			// MSL ANSI definitions		#else			// Obsolete ANSI libraries			#if powerc				#include	<MacHeadersPPC>			// Obsolete headers			#else				#include	<MacHeaders68K>			// Obsolete headers			#endif		#endif /* USE_MSL */				#include "VString.h"						// Missing ANSI string functions	#endif /* macintosh */#endif#ifdef __BORLANDC__				// Borland#endif /* __BORLANDC__ */#define VCLASS#define Macintosh#define RADCON#include "VDefs.h"