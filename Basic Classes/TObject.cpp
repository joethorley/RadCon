#include "TObject.h"// JLT 000624#include "wstream.h"extern ostream *logStream;void TObject::WriteToLog (void) const{	#ifdef VCLASS 		Write (*logStream);		logStream->flush();	#else		Write (cout);	#endif/*	#ifdef VCLASS 		#if (USE_MSL)			ostringstream s;			Write (s);			s << '\0';			string str = s.str();			char *buf = (char *)str.c_str();			*Log << buf;		#else			ostrstream s;			Write (s);			s << '\0';			char *buf = s.str();			*Log << buf;			delete buf;		#endif		#else		Write (cout);	#endif*/} 